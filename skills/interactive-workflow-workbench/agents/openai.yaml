interface:
  display_name: "Interactive Workflow Workbench"
  short_description: "Build readable evidence-backed workflow pages"
  default_prompt: "Use $interactive-workflow-workbench to create a scrollable overview plus separate, human-readable start-to-terminal workflow pages."
